image: Visual Studio 2017

platform:
  - x86
  - Any CPU

configuration:
  - Debug

init:
  - cmd: git config --global core.autocrlf true

build_script:
  - cmd: dotnet --version
  - cmd: cd ./Realmar.Pipes.Tests
  - cmd: dotnet restore
  - cmd: dotnet tests